import { ICON_WALLET_PATH } from '../path';

const assets = {
  addIcon: `${ICON_WALLET_PATH}/add-icon.svg`,
  optionMenuIcon: `${ICON_WALLET_PATH}/ellipsis-vertical-solid.svg`,
};

export const addIcon = assets.addIcon;
export const optionMenuIcon = assets.optionMenuIcon;